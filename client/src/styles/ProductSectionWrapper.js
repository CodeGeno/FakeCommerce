import styled from 'styled-components'

const Wrapper = styled.div`
  width: 100%;
  padding: 0 0.8rem;
  .products-container {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
  }
`

export default Wrapper
